$ preflightkit test pfk-go-graceful --port 8000 --ready-url /ready
starting the traffic probe
starting the target and waiting for readiness: pfk-go-graceful
measuring the baseline: 25 requests plus keep-alive
sending SIGTERM and observing the shutdown
removing the containers and the network

preflightkit 0.1.0   run pfk_T5MX7M2FMQ
Target: pfk-go-graceful
Profile: kubernetes, grace 30s, drain none -> shutdown budget 30s

STARTUP
  startup resolution                          199ms
  TCP :8000 open                              0ms
  GET /ready -> 200                           1ms
  PID 1 signal disposition                    server, SIGTERM handler installed

BASELINE (before the signal)
  GET /ready x25                              25/25 ok -> 200
  request duration p50 / p90                  6ms / 7ms

SHUTDOWN TIMELINE
  T2  last new connection accepted            +-24ms (±50ms)

CONTRACTS
  SP001 startup                PASS   ready in 1ms (budget 15s)
  SP002 readiness-stability    PASS   readiness returned 200 for all 10 probes (p50 0ms, max 0ms)
  SP003 signal-handling        PASS   shutdown started and the process stopped within budget after 
66ms
  SP004 drain-window           WARN   no drain mechanism covers routing propagation; production 
rollouts can send traffic to a process that is shutting down. Add a preStop sleep, or test 
application-owned draining with --drain in_app
        - accept_window is resolved to the 50ms probe interval; it is not a continuous timestamp.
  SP005 inflight-completion    INCONCLUSIVE  readiness p50 0.3ms, jitter 0.2ms, ratio 1.7x is below 
the required 10x; the in-flight window cannot be distinguished from measurement noise — point 
--inflight-path at a slower endpoint
        - 0 of 0 requests finished before the signal. The window closed early: lower 
contracts.inflight.sigterm_after, or leave it unset and let it be derived from the baseline p50.
        - The experiment and traffic measurement completed, but this unresolved candidate was not 
published because its precondition did not hold.
  SP006 shutdown-deadline      PASS   exited in 66ms of 30s

  host Darwin 25.5.0 | docker 29.7.2/linux | 11 cpu | load 5.01 | probe sidecar | probe-path jitter 
~0.2ms | teardown not_calibrated
  Timestamps come from monotonic_ns; application-facing observations are precise to about 0.2ms at 
sidecar, not to the nanosecond.
  T4 is the daemon's own kill->die pair (66ms), one clock with no round trip of ours in it. The 
timeline above is drawn on our clock, which saw the exit at 85ms.

Result: 1 WARN
Warning: 1 required contract did not produce a verdict (1 INCONCLUSIVE). Gating blocks unless 
--allow-inconclusive is set.
report-only: exit 0. Use --fail-on error to block CI.
