version: 1

target:
  image: pfk-fixture-stdlib
  port: 8000
  env:
    HANDLE_SIGTERM: "1"
    READINESS_DELAY_SECONDS: "0.05"

deployment:
  termination_grace_period: 30s

contracts:
  readiness:
    latency_budget: 10ms
